<%= render partial: 'title', locals: { gallery: @gallery } %>

<% @gallery.images.each do |image| %>
  <div class="row">
    <div class="col-xs-12 gallery-item">
      <%= link_to image_tag(image.main_image), @gallery.statement_path %>
    </div>
  </div>
<% end %>
